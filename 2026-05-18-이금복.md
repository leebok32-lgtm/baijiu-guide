# 2026-05-18 일일 정리 — 이금복

## 프로젝트 간단 설명

**고량주가이드** — 한국어로 백주·바이주(중국 증류주) 정보를 제공하는 웹사이트.
사진으로 라벨을 찾고, 향형(청향/농향/장향 등)으로 비교하고, 음식 페어링을 추천한다.
주류 판매는 하지 않는 정보·검색·추천 서비스.

- **스택**: Next.js 15 (App Router) + React 19 + TypeScript + Tailwind CSS
- **데이터**: 현재 mock(`data/products.ts`), 추후 Supabase 전환 (repository 패턴으로 추상화 완료)
- **OCR**: 현재 mock provider, `OCR_PROVIDER` 환경변수로 Google Vision / OpenAI Vision 전환 가능
- **위치**: `C:\Users\admin\Desktop\KSA\Baijiu`

## 오늘 한 일

### 1) Next.js 베이스 + 전체 페이지 (45개 prerender)

- 프로젝트 설정 파일 일체 — `package.json`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.mjs`, `next.config.mjs`
- 타입 정의 — `types/index.ts` (`Product`, `AromaType`, `Pairing`, `Article`, `ProductFilters`, `SortKey`)
- mock 데이터 — `data/products.ts`(22종, 모두 `verified: false`), `data/aromaTypes.ts`(6종), `data/pairings.ts`(9종), `data/articles.ts`(4건)
- 검색·필터 유틸 — `lib/search/searchProducts.ts`, `lib/search/filterProducts.ts`
- mock OCR + 정규화 + 매칭 — `lib/ocr/mockOcr.ts`, `normalizeOcrText.ts`, `matchProduct.ts`
- 레이아웃 — `components/layout/Header.tsx`, `Footer.tsx`
- 공통 컴포넌트 — `AromaBadge`, `DifficultyBadge`, `PriceBadge`, `VerifiedBadge`, `ProductImage`, `PairingCard`, `RankingSection`, `GuideArticleCard`, `SubmitForm`
- 제품 컴포넌트 — `ProductCard`, `ProductDetailHeader`, `ProductInfoTable`, `ProductFlavorCard`, `ProductPairingCard`, `ProductPriceCard`, `ProductStorySection`, `RelatedProductsSection`
- 검색/사진 컴포넌트 — `SearchInput`, `ProductFilterBar`, `PhotoUploadBox`, `PhotoSearchResultCard`
- 모든 라우트 — `app/page.tsx`, `app/guide/`, `app/products/[+slug]`, `app/aroma/[+slug]`, `app/pairing/`, `app/ranking/`, `app/articles/[+slug]`, `app/photo-search/`, `app/submit/`, `app/not-found.tsx`
- `app/layout.tsx` + `app/globals.css` (Pretendard, 크림+남색+금색 팔레트)
- 빌드 검증 — 45페이지 prerender 성공

### 2) Supabase 연동 준비 (repository 패턴)

- 의존성 추가 — `@supabase/supabase-js ^2.50.0`
- `lib/supabase/client.ts` — env var 둘 다 있을 때만 client 반환, 없으면 `null`
- `lib/supabase/database.types.ts` — snake_case `ProductRow`/`AromaTypeRow`/`PairingRow`/`ArticleRow`/`SubmissionRow` + `Database` 타입
- `lib/supabase/mappers.ts` — `rowToProduct`/`productToInsert` + 향형/페어링/아티클 매퍼
- `lib/products/repository.ts` — `ProductRepository` 인터페이스 + `getActiveRepository()` (env 기반 자동 선택)
- `lib/products/mockRepository.ts` + `supabaseRepository.ts`
- public async API 6종 — `getProducts`, `getProductBySlug`, `getProductsByIds`, `getProductsByAroma`, `searchProducts`, `filterProducts`
- 모든 서버 페이지·컴포넌트 마이그레이션 — `app/page.tsx`, `app/products/[slug]/page.tsx`, `app/aroma/[slug]/page.tsx`, `app/ranking/page.tsx`, `app/products/page.tsx`, `app/photo-search/page.tsx`, `components/common/PairingCard.tsx`
- 클라이언트 컴포넌트는 props 수신 — `ProductsExplorer`, `ProductFilterBar`, `PhotoSearchExperience`
- `data/products.ts` 의 helper export 제거 (mockRepository만 사용처)
- `.env.example` 작성 (Supabase 섹션)
- `supabase/schema.sql` — products + aroma_types + pairings + articles + submissions, GIN 인덱스(text[]), `updated_at` 트리거, RLS 정책(공개 SELECT + anon INSERT for submissions)
- 빌드 검증 통과

### 3) OCR multi-provider 고도화

- `lib/ocr/providers/types.ts` — `OcrProvider` 인터페이스 (`name`, `isReady()`, `recognize()`)
- `lib/ocr/providers/mockProvider.ts` — 기본값, 5개 라벨 무작위
- `lib/ocr/providers/googleVisionProvider.ts` — REST API + `GOOGLE_VISION_API_KEY`, `DOCUMENT_TEXT_DETECTION`, `languageHints: ["zh","ko","en"]`, 블록 단위 추출
- `lib/ocr/providers/openaiVisionProvider.ts` — Chat Completions + vision, `OPENAI_API_KEY`(+ 선택 `OPENAI_MODEL`, 기본 `gpt-4o-mini`), 한국어 system prompt
- `lib/ocr/providers/index.ts` — `getActiveOcrProvider()` 가 env 읽고 key 검증, 누락 시 mock fallback + `fallbackFrom` 반환
- `app/api/ocr/route.ts` — POST multipart, 8MB 한도, `image/*` 검증, 메모리 처리만(no disk write), `force-dynamic`
- `lib/ocr/normalizeOcrText.ts` 개선 — full-width 숫자 정규화, CJK 2/3-char n-gram, 도수 패턴 6종(`%`/`vol`/`도`/`度`/`abv`/`alc`), 용량 패턴(`ml`/`毫升`/`cl`/`L`→ml)
- `lib/ocr/matchProduct.ts` 개선 — 3-tier 점수(substring → CJK n-gram → Latin word), 동일 필드 중복 가산 차단, ±1% 도수 근사 일치, `confidence: 0..100`, `MATCH_CONFIDENCE_THRESHOLD = 35`
- `app/photo-search/PhotoSearchExperience.tsx` — `mockOcr()` 직접 호출 제거 → `POST /api/ocr` 로 전환, provider 라벨 + `fallbackFrom` ⚠ 안내, **프라이버시 안내 박스 추가**
- `components/photo/PhotoSearchResultCard.tsx` — `유사도 NN%` + raw score 표시
- `.env.example` 에 OCR 섹션 추가
- `lib/ocr/mockOcr.ts` 삭제 (mockProvider로 통합)
- 빌드 검증 — `ƒ /api/ocr` dynamic route 정상

### 4) End-of-day 스킬 생성

- 사용자 이름 메모리 저장 — `~/.claude/projects/.../memory/user_profile.md`
- 스킬 작성 — `~/.claude/skills/end-of-day/SKILL.md` (트리거 + 템플릿 + 작성 가이드)

## 논의한 내용

- **클라이언트 컴포넌트는 데이터 fetch 금지** — 항상 부모 서버 컴포넌트가 `await` 후 props로 전달. 이유: ① supabase-js / OCR API key 클라이언트 노출 방지, ② 클라이언트 번들 사이즈, ③ RSC 패턴 정합성. ProductsExplorer/PhotoSearchExperience가 이 원칙대로 리팩토링됨.
- **OCR API 호출은 반드시 서버** — `NEXT_PUBLIC_` 없는 키를 보호하기 위해 `/api/ocr` 경유. 이미지는 메모리(`arrayBuffer` → base64 → provider)에서만 흐르고 디스크/DB 어디에도 저장 금지(개인정보·라벨 사진 보관 이슈).
- **Repository / Provider 패턴 공통 원칙**: 환경변수 없으면 에러 대신 mock fallback. dev 환경에서도 항상 동작 + UI 에 fallback 발생 시 ⚠ 안내.
- **supabase-js Database 타입 함정** — 각 테이블에 `Relationships: []`, 스키마에 `CompositeTypes`/`Views`/`Functions` 가 모두 `{ [_ in never]: never }` 형태로 있어야 `data` 가 `never[]` 로 안 잡힘. 공식 `supabase gen types typescript` 출력과 동일한 shape 유지 필요.
- **`new Map(arr.map(r => [k, v]))` 추론 함정** — Map 생성자의 컨텍스트 타입이 `[r.id, ...]` 를 튜플 대신 배열로 추론해서 `K,V` 가 `never` 가 됨. 해결: 명시적 `for...of`로 풀어 쓰기 (`supabaseRepository.getByIds` 참고).
- **OCR 3-tier 매칭** — substring(완전) → CJK n-gram(부분 라벨) → Latin 단어(병음/브랜드). 한 필드당 한 tier 점수만 채택해 중복 가산 차단. CJK n-gram 덕에 OCR이 "茅台" 만 잡아도 "贵州茅台酒" 매칭 가능.
- **mock 데이터 직접 import 금지 정책** — 모든 제품 조회는 `lib/products/*` 경유. `data/products.ts` 의 helper 함수는 제거, mockRepository만이 유일한 사용처.

## 다음 할 일

- [ ] Supabase 실제 프로젝트 생성 + `supabase/schema.sql` 실행
- [ ] mock 데이터를 실제 DB로 옮기는 시드 스크립트 작성 (예: `scripts/seed.ts`, `productToInsert` 활용)
- [ ] `/submit` 폼을 mock console.log → 실제 `submissions` 테이블 insert로 연결
- [ ] 제품 이미지 파일 마련 (현재 `ProductImage` SVG fallback) 또는 Supabase Storage 연동
- [ ] `OCR_PROVIDER=google` 실제 키로 시연 — `GOOGLE_VISION_API_KEY` 발급 후 라벨 1~2장 테스트
- [ ] `OCR_PROVIDER=openai` 실제 키로 시연 — `gpt-4o-mini` 비용/정확도 확인
- [ ] (선택) `/search?q=...` 별도 결과 라우트 검토
- [ ] (선택) i18n 도입 검토 (중국어/영어 버전)
- [ ] end-of-day 스킬 실제 사용 — `/reload-plugins` 또는 세션 재시작 후 트리거 동작 확인
